name: flutter_tide
description: A very thin layer on top of state_notifier and provider to give guidelines when architecturing an application.
version: 0.2.1
homepage: https://github.com/aloisdeniel/tide

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  provider: ^4.3.2+2
  state_notifier: ^0.6.0
  flutter_state_notifier: ^0.6.1
  tide: ^0.2.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
